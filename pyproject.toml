[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "evez-sdk"
version = "1.0.0"
description = "Python SDK for the EVEZ microservice mesh"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["evez*"]
